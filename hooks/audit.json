{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "if git status --porcelain | grep -q 'package.*\\.json'; then npm audit --audit-level=high; fi" }
        ]
      }
    ]
  }
}
